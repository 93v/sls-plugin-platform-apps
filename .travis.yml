language: generic
install:
  - DEBUG=true npm install
before_deploy:
  - npm run lint
  - npm run test
  - npm run build
  - cd dist
deploy:
  provider: npm
  email: "&NPM_EMAIL"
  api_key: "$NPM_TOKEN"
  on:
    tags: true
